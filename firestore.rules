service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Reemplaza ADMIN_UID_REEMPLAZAR con el UID del administrador autorizado
      allow read, write:
          if request.auth != null && request.auth.uid == "ADMIN_UID_REEMPLAZAR";
    }
  }
}
